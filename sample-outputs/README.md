# Sample output

`safety-report.json` is a representative structured response for the first synthetic record. The project’s AI service returns the same shape for each queued message: category, confidence, reasons, source trace, table/image hints, and extracted facts. Missing values are intentionally left as `Not stated` rather than guessed.
